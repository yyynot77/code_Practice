# 2026-06-12 Day 2 Oral-Proof-First

## Training Position

- Date: 2026-06-12
- Plan day: Day 2 / 30
- Status: `BLOCKED`
- Escalation: eighth strict Day 2 intervention
- Reason: the same Day 2 hash and prefix invariants still do not have a recorded clean clear, so breadth is reduced and proof pressure is increased.
- Focus: equivalence key, fixed alphabet counting, sequence start, prefix frequency, earliest index, normalized modulo

## Gate Set

| Problem | Difficulty | Pattern | Proof target | Coding target | Result |
| --- | --- | --- | --- | --- | --- |
| 49. Group Anagrams | Medium | Count-vector equivalence key | Explain why equal multisets map to one stable key in 90 sec | AC in 12 min + rewrite in 4 min | `UNFINISHED` |
| 242. Valid Anagram | Easy | Fixed alphabet frequency array | Explain why counting beats sorting here in 45 sec | AC in 6 min + rewrite in 3 min | `UNFINISHED` |
| 128. Longest Consecutive Sequence | Medium | HashSet + start-only expansion | Explain why only starts expand and why that stays linear in 90 sec | AC in 12 min | `UNFINISHED` |
| 560. Subarray Sum Equals K | Medium | Prefix sum + frequency hash | Explain query-before-insert in 90 sec | AC in 15 min | `UNFINISHED` |
| 525. Contiguous Array | Medium | Prefix balance + earliest index hash | Explain earliest-index retention in 90 sec | AC in 18 min | `UNFINISHED` |
| 974. Subarray Sums Divisible by K | Medium | Prefix modulo + frequency hash | Explain normalized modulo in 90 sec | AC in 18 min | `UNFINISHED` |
| 383. Ransom Note | Easy | Counting comparison drill | Only if `49` or `242` fails | AC in 5 min | `UNFINISHED` |
| 930. Binary Subarrays With Sum | Medium | Prefix-frequency comparison drill | Only if `560`, `525`, or `974` fails | AC in 15 min | `UNFINISHED` |

## Hard Rules

1. The oral proof must happen before code for every gate.
2. Proof content must include invariant, data structure choice, and complexity argument.
3. Editorial-assisted solutions do not count as pass.
4. After each pass, rewrite the template from memory without looking.
5. If one count-key gate fails, do `383` immediately.
6. If one prefix-hash gate fails, do `930` immediately.
7. If two or more gates fail again, Day 3 remains locked and tomorrow stays on Day 2.

## Core Invariants

- `49`: equivalent letter multisets must serialize to the same stable key.
- `242`: a fixed lowercase alphabet allows direct count comparison in constant extra space.
- `128`: only numbers without predecessors are allowed to start expansion.
- `560`: the answer added at index `i` equals prior occurrences of `prefix[i] - k`.
- `525`: the first time a balance appears gives the longest later span with the same balance.
- `974`: equal normalized modulo states imply a difference divisible by `k`.

## Frequent Failure Points

- Using memorized code without naming the invariant.
- Mixing up query-before-insert in `560`.
- Overwriting earliest state in `525`.
- Forgetting modulo normalization in `974`.
- Saying `O(n)` for `128` without defending it.

## Complexity Check

- `49`: `O(n * m)` time, `O(n * m)` space
- `242`: `O(m)` time, `O(1)` space
- `128`: `O(n)` time, `O(n)` space
- `560`: `O(n)` time, `O(n)` space
- `525`: `O(n)` time, `O(n)` space
- `974`: `O(n)` time, `O(min(n, k))` space

## Review Questions

1. Why is the frequency vector a valid canonical representation for `49`?
2. Why does `242` not need sorting?
3. What is the exact start predicate in `128`?
4. Why does `560` query before insert?
5. Why does `525` store the first index only?
6. Why does `974` need normalized modulo?

## Carry-Over

- Day 3 opens only after all six gates are marked `PASS`.
- If count-key gates fail again, add one more counting problem tomorrow.
- If prefix-hash gates fail again, add one more prefix-state problem tomorrow.
- If two or more gates fail again, tomorrow remains oral-proof-first Day 2.
