# 2026-06-19 Day 2 Dual-Template Compression Rebuild

## Training Position

- Date: 2026-06-19
- Plan day: Day 2 / 30
- Status: `BLOCKED`
- Escalation: thirteenth strict Day 2 intervention
- Reason: the 2026-06-18 record still shows every mandatory row unresolved, so the gate set contracts again to the smallest template rebuild that still covers both weak families.
- Focus: seal one count-signature family and one prefix-frequency family before any wider Day 2 unlock
- Weak family A: count-signature hashing
- Weak family B: prefix-frequency hashing

## Gate Set

| Order | Problem | Difficulty | Pattern | Proof target | Coding target | Result | Actual time |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Primer A | 242. Valid Anagram | Easy | Fixed alphabet frequency array | Define the 26-slot array as a canonical multiset signature in 30 sec | AC in 6 min | `UNFINISHED` | `TBD` |
| Gate 1 | 49. Group Anagrams | Medium | Count-vector canonical key | Define the invariant and defend the key as an equivalence-class representation in 90 sec | AC in 12 min + template rewrite in 4 min | `UNFINISHED` | `TBD` |
| Primer B | 930. Binary Subarrays With Sum | Medium | Prefix sum + frequency hash | Define `count[prefix]` and defend query-before-insert in 45 sec | AC in 15 min | `UNFINISHED` | `TBD` |
| Gate 2 | 560. Subarray Sum Equals K | Medium | Prefix sum + frequency hash | Define the hash meaning and explain query-before-insert in 90 sec | AC in 15 min + template rewrite in 4 min | `UNFINISHED` | `TBD` |
| Locked Gate | 525. Contiguous Array | Medium | Prefix balance + earliest index hash | Locked until all four mandatory rows pass on the same date | `LOCKED` | `TBD` | `TBD` |
| Locked Release | 974. Subarray Sums Divisible by K | Medium | Prefix modulo + frequency hash | Locked until `525` is later cleared after the four-row set | `LOCKED` | `TBD` | `TBD` |

## Hard Rules

1. Oral proof first, code second, template rewrite third.
2. Every proof must include invariant, state definition, update order, and complexity.
3. `242` and `49` are one family block. Do not leave count-signature hashing half-clear and move on.
4. `930` is mandatory before `560` because the prefix-frequency template is still unstable.
5. `525` and `974` are locked today because repeated failures show that widening the set only hides the core weakness.
6. No editorials on primers or gates. Editorial exposure means the row stays `UNFINISHED`.
7. Replace `UNFINISHED`, `LOCKED`, and `TBD` with actual outcomes before ending the session.

## Core Templates

### Canonical Count Signature

```java
int[] freq = new int[26];
for (char c : s.toCharArray()) {
    freq[c - 'a']++;
}
String key = Arrays.toString(freq);
```

### Prefix Frequency

```java
Map<Integer, Integer> count = new HashMap<>();
count.put(0, 1);
int prefix = 0;
for (int num : nums) {
    prefix += num;
    ans += count.getOrDefault(prefix - target, 0);
    count.put(prefix, count.getOrDefault(prefix, 0) + 1);
}
```

## Frequent Failure Points

- Calling the map "for lookup" without naming the stored state.
- Treating `242` and `49` as string tricks instead of multiset-signature design.
- Querying after insert in `930` or `560`.
- Forgetting that `count[prefix]` counts earlier prefixes, not subarrays.
- Trying to unlock `525` or `974` before the smaller four-row gate is clean on the same date.

## Review Questions

1. In one sentence, what does the count signature represent in `242` and `49`?
2. In one sentence, what does `count[prefix]` represent in `930` and `560`?
3. Why must the prefix query happen before the insert?
4. What exact condition must be met before `525` is unlocked again?
5. Why is today's narrower gate set stricter than yesterday's wider set?

## Carry-Over Rule

- Any unfinished mandatory row keeps the plan on Day 2.
- Another `242` or `49` failure upgrades count-signature hashing to the first handwritten drill tomorrow.
- Another `930` or `560` failure upgrades prefix-state comparison to the first handwritten drill tomorrow.
- If two or more mandatory rows remain unfinished, tomorrow repeats the same four-row gate with lower time caps.
- A clean pass on all four mandatory rows unlocks `525` for the next run, but `974` stays locked behind it.
