# 2026-06-10 Day 2 阻塞闸门复训

## 训练定位

- 日期：2026-06-10
- 计划天数：Day 2 / 30
- 状态：`BLOCKED`
- 原因：2026-06-09 没有留下六个核心闸门题全部独立通过的明确记录，因此今天不能进入 Day 3。
- 今日主题：哈希等价类键、定长频次数组、序列起点检测、前缀和频次哈希、前缀平衡态、前缀取模态

## 今日闸门题

| 顺序 | 题目 | 难度 | 模式 | 通过标准 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | 49. Group Anagrams | Medium | 计数向量等价键 | 12 分钟内 AC + 4 分钟默写模板 | `UNFINISHED` |
| 2 | 242. Valid Anagram | Easy | 定长频次数组 | 6 分钟内 AC + 30 秒解释为何不用排序 | `UNFINISHED` |
| 3 | 128. Longest Consecutive Sequence | Medium | HashSet 只从起点扩展 | 12 分钟内 AC + 45 秒证明线性复杂度 | `UNFINISHED` |
| 4 | 560. Subarray Sum Equals K | Medium | 前缀和 + 频次哈希 | 15 分钟内 AC + 45 秒解释先查后放 | `UNFINISHED` |
| 5 | 525. Contiguous Array | Medium | 前缀平衡值 + 最早下标 | 18 分钟内 AC + 45 秒解释为什么保留最早下标 | `UNFINISHED` |
| 6 | 974. Subarray Sums Divisible by K | Medium | 前缀取模 + 频次哈希 | 18 分钟内 AC + 45 秒解释模归一化 | `UNFINISHED` |

## 处罚加练触发条件

- 如果 `49` 或 `242` 任一未完成，立刻加练 `383. Ransom Note`。
- 如果 `560`、`525`、`974` 任一未完成，立刻加练 `930. Binary Subarrays With Sum`。
- 处罚加练也必须记录 `PASS` 或 `UNFINISHED`，但不会替代核心闸门题结果。

## 严格执行规则

1. 六道闸门题总时长上限 120 分钟，超时立即停。
2. 闸门题禁止看题解；看过题解后即使 AC 也记为 `UNFINISHED`。
3. 每题 AC 后必须关闭编辑器，5 分钟内默写对应模板。
4. 每题必须口述三句话：时间复杂度、空间复杂度、关键不变量。
5. 今天结束前必须把本文件中的结果列改成真实结果，不能保留空白状态。

## 核心模板

### 1. 等价类计数键

```java
Map<String, List<String>> groups = new HashMap<>();
for (String s : strs) {
    int[] freq = new int[26];
    for (char c : s.toCharArray()) {
        freq[c - 'a']++;
    }
    String key = Arrays.toString(freq);
    groups.computeIfAbsent(key, k -> new ArrayList<>()).add(s);
}
```

### 2. 定长频次数组

```java
int[] diff = new int[26];
for (int i = 0; i < s.length(); i++) {
    diff[s.charAt(i) - 'a']++;
    diff[t.charAt(i) - 'a']--;
}
for (int value : diff) {
    if (value != 0) {
        return false;
    }
}
return true;
```

### 3. 只从序列起点扩展

```java
Set<Integer> set = new HashSet<>();
for (int num : nums) {
    set.add(num);
}
for (int num : set) {
    if (!set.contains(num - 1)) {
        int current = num;
        int length = 1;
        while (set.contains(current + 1)) {
            current++;
            length++;
        }
        best = Math.max(best, length);
    }
}
```

### 4. 前缀频次哈希

```java
Map<Integer, Integer> count = new HashMap<>();
count.put(0, 1);
int prefix = 0;
for (int num : nums) {
    prefix += num;
    ans += count.getOrDefault(prefix - target, 0);
    count.put(prefix, count.getOrDefault(prefix, 0) + 1);
}
```

### 5. 前缀平衡态

```java
Map<Integer, Integer> first = new HashMap<>();
first.put(0, -1);
int balance = 0;
for (int i = 0; i < nums.length; i++) {
    balance += nums[i] == 1 ? 1 : -1;
    if (first.containsKey(balance)) {
        best = Math.max(best, i - first.get(balance));
    } else {
        first.put(balance, i);
    }
}
```

### 6. 前缀取模态

```java
Map<Integer, Integer> modCount = new HashMap<>();
modCount.put(0, 1);
int prefix = 0;
for (int num : nums) {
    prefix += num;
    int mod = ((prefix % k) + k) % k;
    ans += modCount.getOrDefault(mod, 0);
    modCount.put(mod, modCount.getOrDefault(mod, 0) + 1);
}
```

## 高频错误

- 把 `49` 的 key 当成死记硬背技巧，而不是“同一字母多重集映射到同一表示”。
- 在 `242` 上默认排序，没有先判断“字母表固定”这个条件。
- 在 `128` 中从每个数字都开始扩展，导致论证和实现都失真。
- 在 `560` 中先把当前前缀放入哈希，再去查询目标差值。
- 在 `525` 中覆盖最早下标，导致最长区间答案丢失。
- 在 `974` 中直接使用负模值当 key。

## 复杂度速记

- `49`：时间 `O(n * m)`，空间 `O(n * m)`
- `242`：时间 `O(m)`，空间 `O(1)`
- `128`：时间 `O(n)`，空间 `O(n)`
- `560`：时间 `O(n)`，空间 `O(n)`
- `525`：时间 `O(n)`，空间 `O(n)`
- `974`：时间 `O(n)`，空间 `O(min(n, k))`

## 复盘追问

1. 为什么 26 长度频次数组可以作为 `49` 的等价类键？
2. `242` 为什么优先用计数而不是排序？
3. `128` 的“序列起点”判定条件是什么？
4. `128` 的 `while` 循环为什么总复杂度仍是线性的？
5. `560` 为什么必须先查 `prefix - k` 再插入当前前缀？
6. `525` 为什么要存某个平衡值第一次出现的位置？
7. `974` 为什么必须做模归一化？

## 明日结转

- 六道闸门题全部 `PASS` 才允许 2026-06-11 进入 Day 3 滑动窗口。
- 只要 `49` 或 `242` 仍未完成，明天继续补一题计数比较题。
- 只要 `560`、`525`、`974` 中还有未完成，明天继续补一题前缀哈希比较题。
- 如果两个族群都未清空，明天继续 Day 2，并降低题量但提高复述要求。
