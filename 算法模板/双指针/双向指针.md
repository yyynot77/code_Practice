```
int left = 0, right = n - 1;

while (left < right) {

    // 1️⃣ 基于当前 left / right 处理结果
    if (当前构成的解满足条件) {
        更新答案 / return;
    }

    // 2️⃣ 根据题目的单调规则移动指针
    if (移动左指针更合理) {
        left++;
        // 可选：剪枝
        while (left < right && left 不可能产生更优解) {
            left++;
        }
    } else {
        right--;
        // 可选：剪枝
        while (left < right && right 不可能产生更优解) {
            right--;
        }
    }
}

```