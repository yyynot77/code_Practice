# 哈希表基础模板

## 1. 值到下标映射

适用: `Two Sum`、是否存在配对、一次遍历找互补值

```java
Map<Integer, Integer> map = new HashMap<>();
for (int i = 0; i < nums.length; i++) {
    int need = target - nums[i];
    if (map.containsKey(need)) {
        return new int[] { map.get(need), i };
    }
    map.put(nums[i], i);
}
```

要点:

- 先查再放，防止同一个元素被重复使用。
- `map` 存的是“之前见过的信息”，不是“当前所有信息”。
- `key` 是数值，`value` 是下标。

## 2. 分组类哈希

适用: 字母异位词、同构分类、特征签名分组

```java
Map<String, List<String>> groups = new HashMap<>();
for (String s : strs) {
    int[] freq = new int[26];
    for (char c : s.toCharArray()) {
        freq[c - 'a']++;
    }
    String key = Arrays.toString(freq);
    groups.computeIfAbsent(key, ignored -> new ArrayList<>()).add(s);
}
return new ArrayList<>(groups.values());
```

要点:

- `key` 必须稳定: 同一类对象得到完全相同的 key。
- 当前冲刺阶段优先练计数签名 key，而不是排序 key。

## 3. 去重 + 只从起点扩展

适用: 最长连续序列、只从起点扩展的集合题

```java
Set<Integer> set = new HashSet<>();
for (int x : nums) {
    set.add(x);
}

int best = 0;
for (int x : set) {
    if (!set.contains(x - 1)) {
        int y = x;
        while (set.contains(y)) {
            y++;
        }
        best = Math.max(best, y - x);
    }
}
return best;
```

要点:

- 只从起点扩展，每个元素最多被 `while` 访问一次。
- 证明复杂度时，要明确说明“每个元素只进入一段序列一次”。

## 今日硬性检查

- 能否不看答案写出三个模板?
- 能否解释 key / 去重 / 起点判断各自解决了什么问题?
- 能否在 3 分钟内说清对应复杂度?
